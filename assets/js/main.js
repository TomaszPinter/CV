
AOS.init();


let options = {
    startDelay: 500,
    strings: ["FRONTEND DEWELOPER", "BACKEND DEWELOPER", "FULLMASTER DEWELOPER"],
    typeSpeed: 40,
    loop: true,
};

let typed = new Typed('.typed', options);